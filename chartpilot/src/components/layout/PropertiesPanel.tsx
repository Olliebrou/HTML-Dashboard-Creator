import Panel from '../common/Panel';

export default function PropertiesPanel() {
  return (
    <div className="cp-properties">
      <Panel title="Properties">
        <p className="cp-muted">Select an element on the canvas to edit its settings.</p>
      </Panel>
    </div>
  );
}
